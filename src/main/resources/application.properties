spring.application.name=ProjectTracker

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/project_tracker_db
spring.datasource.username=postgres
spring.datasource.password=728728
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# MongoDB Configuration (for Audit Logs)
spring.data.mongodb.uri=mongodb://localhost:27017/project_tracker_audit_db


# SpringDoc OpenAPI (Swagger UI) Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=buntulevycaleb@gmail.com
spring.mail.password=xopf pgtd yrvv fdky 
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
# spring.mail.properties.mail.smtp.ssl.trust=your-smtp-host (if using self-signed certs)

# --- Application Specific Email Settings ---
# Email address from which notifications will be sent
notification.email.from=buntulevycaleb@gmail.com
# Subject prefix for notification emails
notification.email.subjectPrefix=[ProjectTracker]
